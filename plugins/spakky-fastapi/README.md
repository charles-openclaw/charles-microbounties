# Spakky FastAPI Plugin

The Spakky FastAPI plugin provides integration with the FastAPI framework.

## Setup

To use the Spakky FastAPI plugin, you need to manually register a `FastAPI` instance as a Pod. Here's an example:
