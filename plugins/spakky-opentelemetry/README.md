# OpenTelemetry Plugin for Spakky Framework

## Usage

To use the OpenTelemetry plugin, you need to create an instance of `LogContextBridge` and call its `sync` method after setting the `TraceContext`. Here's an example:
