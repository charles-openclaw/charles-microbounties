import { VERSION } from './version';

// ... rest of the code remains the same ...

const server = new McpServer({
  name: 'anchor-debug',
  version: VERSION,
  // ... rest of the code remains the same ...
});